apiVersion: v1
kind: Service
metadata:
    name: "kubernetesdemo"
    labels:
        app: "kubernetesdemo"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "kubernetesdemo"